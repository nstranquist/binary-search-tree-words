# 4280 P0 - Project 1 - Nico Stranquist

### Steps to run this project:
1. Navigate to the project root
2. Make sure g++ or other C/C++ compiler is installed
3. Run `make` in your terminal and the program will be compiled using `Makefile`
4. Run `./main` followed by optional arguments if desired
